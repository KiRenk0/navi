# Faceted3D v2 Recommendation (from P0.2 sandbox)

> Generated: 2026-06-28 23:54
> Based on: Ma=6, α=5°, h=30km, Tw=300K

## Current status

- Busemann Cp overpredicts Fluent Cp by **4.88x** (global mean).
- The overprediction is **region-dependent**: worst on the windward body, aft body, and leading edge; nose region is relatively better.
- **No downstream pressure relaxation**: Fluent pressure drops by ~10x from nose to tail; Faceted3D p_e drops <2x.
- phi range is narrow (14.6°–22.2°), indicating facet normals do not capture geometry-driven expansion.

## Recommended Faceted3D v2 upgrade path

### Phase 1 (immediate, no solver change)
1. Confirm these findings on 2–3 additional Fluent cases (different Ma/α/h).
2. If consistent, design a **region-aware Cp correction** function that can be applied as a post-processing layer in the edge-state chain.

### Phase 2 (solver-level, minimum viable upgrade)
1. **Replace Busemann Cp with a Fluent-calibrated Cp model** for this vehicle class.
   - Option A: Newtonian-like `Cp = A * sin(phi)^n` fitted per region.
   - Option B: x-dependent relaxation `R(x)` modulating Busemann Cp.
   - Option C: region-specific scale factors.
2. **Add a geometry-driven x-dependent pressure relaxation** to simulate downstream expansion.

### Phase 3 (if needed)
3. If pressure-corrected q still deviates, investigate Re_x / development length.
4. Only then consider full 3D surface streamline tracking.

## What NOT to do

- Do NOT apply global Cp scaling — it damages the nose region.
- Do NOT implement full 3D surface streamline tracking for pressure fix — the pressure issue is a Cp model problem, not a streamline problem.
- Do NOT rush to Fluent residual learning before the Cp correction is validated across multiple cases.

## Timing for Fluent residual learning

**Defer until**:
1. Cp correction is validated on 3+ Fluent cases
2. Post-correction residual is verified to be approximately Gaussian / featureless
3. OR at minimum, the correction layer is implemented as a differentiable module

---

*Generated by `scripts/cp_pressure_correction_sandbox.py`*
