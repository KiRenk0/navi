---
description: 项目提交、推送与恢复标签约定
alwaysApply: true
---

# 版本管理约定

- 每次完成一组可独立验证的项目修改后，先检查变更范围，避免提交无关文件、缓存、凭据或密钥。
- 运行与本次修改相关的验证；验证失败时不得把该版本标记为稳定恢复点。
- 用户已授权：修改完成后创建 Git 提交，无需再次询问是否提交。
- 提交消息使用简洁中文，说明修改目的，不使用含糊的“更新代码”。
- 每次成功提交后创建带说明的恢复标签，格式为 `recovery-YYYYMMDD-HHmm-简短主题`。
- 标签必须指向本次提交；同一分钟重名时追加 `-2`、`-3`。
- 提交与标签创建成功后，推送当前分支及该标签到 `origin`。
- 网络或认证失败时保留本地提交和标签，并明确告知用户尚未同步远端。
- 不使用 `git reset --hard`、强制推送或覆盖历史；恢复操作先征得用户确认。