---
description: Git 仓库身份、工作区、提交、推送与恢复安全规则
alwaysApply: true
---

# Git 与工作区安全规则

## 1. 写入前必须认证当前仓库

任何修改、恢复、删除、提交或生成正式资产之前，必须执行并记录：

- `git rev-parse --show-toplevel`
- `git remote get-url origin`
- `git branch --show-current`
- `git rev-parse HEAD`
- `git status --short`

只有同时满足以下条件才能继续：

- Git 根目录就是当前打开的项目根目录；
- `origin` 指向 `KiRenk0/navi`；
- 当前目录包含正式入口、`src/`、`scripts/`、`tests/`、`specs/`；
- 没有来源不明的未提交修改。

若任一条件不满足，立即停止并报告。不得自行选择同级“副本”、空壳目录、临时目录或最接近的历史版本。

禁止为了继续任务而在未知目录执行 `git init`。

## 2. 工作区与导入路径

运行 Python 测试、CLI 或 regression 前，必须确认导入的是当前仓库：

```powershell
$env:PYTHONPATH = (Resolve-Path .\src)
python -c "import pathlib, ref_enthalpy_method; print(pathlib.Path(ref_enthalpy_method.__file__).resolve())"
```

输出路径必须位于当前 Git 根目录的 src/ref_enthalpy_method。

若 Python 导入到其他磁盘、旧副本、editable installation 或历史工作区，立即停止，不得继续验证。

## 3. 修改范围
只修改用户明确授权的文件和职责范围。
不使用 git add -A 或 git add . 盲目暂存全部变化。
不把临时脚本、缓存、日志、凭据、环境文件或一次性诊断资产提交到仓库。
临时脚本用完必须删除。
不把旧副本整体覆盖到当前仓库。
不把 regression PASS 当成未执行 gate 的证明。
不得未经单独授权执行 git add --renormalize . 或全仓库格式化。

对于 baseline、manifest 和 summary，必须分别区分：

数值与字段合同；
parsed semantic contract；
raw artifact hash；
provenance path。
## 4. 分支、提交与推送

用户明确要求修改代码或文档时：

优先从当前已认证 HEAD 创建独立任务分支；
除非用户明确授权，不直接修改 main；
修改完成后先运行相关 tests 和 regression；
验证通过后可提交并推送任务分支，无需再次询问；
合并到 main、打 tag、删除分支或改写历史仍需用户明确授权。

提交前必须执行：

git diff --check
git status --short
git diff --stat
git diff --cached --stat
git diff --cached

只暂存本轮授权文件。

提交信息使用明确中文，说明实际修改目的，不使用“更新代码”等含糊描述。

## 5. 恢复点与标签

普通提交本身就是可恢复点，不为每个提交自动创建标签。

只有在用户明确宣布：

baseline freeze；
schema promotion；
release；
重要物理合同冻结；

时，才创建 annotated tag。

标签格式：

baseline-YYYYMMDD-简短主题

标签必须指向已经验证通过并推送的提交。

## 6. 禁止的 Git 操作

未经用户明确授权，禁止：

git reset --hard
git clean -fd 或 git clean -fdx
git push --force
git push --force-with-lease
覆盖、删除或重写远端历史
删除远端分支或标签
修改已经发布提交
用 manifest 更新掩盖源码或数值漂移
修改全局 Git 配置

出现冲突、异常 dirty state、remote 不一致或认证失败时，保留现场并停止。

## 7. 行尾与冻结资产

活动源码、配置和文档按 .gitattributes 使用 LF。

禁止对以下目录进行无差别行尾重写或全仓库格式化：

runs/
fluent_export/
baseline artifacts
原始 CFD 数据
NPZ、STL 和其他二进制资产

修改 .gitattributes 后，只允许在用户明确授权的独立任务中执行受控 renormalize，并在提交前检查完整 diff。

## 8. 任务完成报告

每轮提交和推送后必须报告：

Git 根目录；
branch；
commit SHA；
push 是否成功；
修改文件清单；
tests 和 regression 结果；
是否仍有未提交文件；
是否修改 baseline、manifest 或 artifact hash。
